# 音效系统实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为银古客栈添加平台背景音乐和游戏音效系统

**Architecture:**
- AudioContext (React Context) 管理全局音频状态，支持音乐开关、音效开关、音量控制
- MusicToggle 悬浮按钮在左下角控制音乐
- GameAudioBridge 处理游戏 iframe 发来的音效请求
- 音频设置持久化到 localStorage
- 页面不可见时自动暂停音乐

**Tech Stack:** React Context, Web Audio API, localStorage, postMessage

---

## Task 1: 创建 AudioContext

**Files:**
- Create: `contexts/AudioContext.tsx`

- [ ] **Step 1: 创建 AudioContext 文件**

```typescript
'use client'

import { createContext, useContext, useState, useEffect, useRef, useCallback, ReactNode } from 'react'

type SoundType = 'click' | 'success' | 'error' | 'achievement'

interface AudioContextType {
  musicOn: boolean
  sfxOn: boolean
  volume: number
  toggleMusic: () => void
  toggleSfx: () => void
  setVolume: (v: number) => void
  playSound: (sound: SoundType) => void
}

const AudioContext = createContext<AudioContextType | null>(null)

const STORAGE_KEYS = {
  music: 'ginko-audio-music',
  sfx: 'ginko-audio-sfx',
  volume: 'ginko-audio-volume',
} as const

export function AudioProvider({ children }: { children: ReactNode }) {
  const [musicOn, setMusicOn] = useState(true)
  const [sfxOn, setSfxOn] = useState(true)
  const [volume, setVolumeState] = useState(50)
  const bgAudioRef = useRef<HTMLAudioElement | null>(null)
  const sfxAudioRef = useRef<Record<SoundType, HTMLAudioElement>>({
    click: new Audio('/audio/sfx/click.mp3'),
    success: new Audio('/audio/sfx/success.mp3'),
    error: new Audio('/audio/sfx/error.mp3'),
    achievement: new Audio('/audio/sfx/achievement.mp3'),
  })

  // Initialize from localStorage
  useEffect(() => {
    const storedMusic = localStorage.getItem(STORAGE_KEYS.music)
    const storedSfx = localStorage.getItem(STORAGE_KEYS.sfx)
    const storedVolume = localStorage.getItem(STORAGE_KEYS.volume)

    if (storedMusic !== null) setMusicOn(storedMusic === 'on')
    if (storedSfx !== null) setSfxOn(storedSfx === 'on')
    if (storedVolume !== null) setVolumeState(Number(storedVolume))
  }, [])

  // Setup background audio
  useEffect(() => {
    if (!bgAudioRef.current) {
      bgAudioRef.current = new Audio('/audio/background.mp3')
      bgAudioRef.current.loop = true
      bgAudioRef.current.volume = volume / 100
    }
    return () => {
      bgAudioRef.current?.pause()
      bgAudioRef.current = null
    }
  }, [])

  // Handle music toggle
  useEffect(() => {
    const audio = bgAudioRef.current
    if (!audio) return

    if (musicOn) {
      audio.play().catch(() => {})
    } else {
      audio.pause()
    }
  }, [musicOn])

  // Handle volume change
  useEffect(() => {
    if (bgAudioRef.current) {
      bgAudioRef.current.volume = volume / 100
    }
  }, [volume])

  // Visibility change: pause when hidden
  useEffect(() => {
    const handleVisibilityChange = () => {
      const audio = bgAudioRef.current
      if (!audio) return
      if (document.hidden) {
        audio.pause()
      } else if (musicOn) {
        audio.play().catch(() => {})
      }
    }
    document.addEventListener('visibilitychange', handleVisibilityChange)
    return () => document.removeEventListener('visibilitychange', handleVisibilityChange)
  }, [musicOn])

  const toggleMusic = useCallback(() => {
    setMusicOn(prev => {
      const next = !prev
      localStorage.setItem(STORAGE_KEYS.music, next ? 'on' : 'off')
      return next
    })
  }, [])

  const toggleSfx = useCallback(() => {
    setSfxOn(prev => {
      const next = !prev
      localStorage.setItem(STORAGE_KEYS.sfx, next ? 'on' : 'off')
      return next
    })
  }, [])

  const setVolume = useCallback((v: number) => {
    setVolumeState(v)
    localStorage.setItem(STORAGE_KEYS.volume, String(v))
  }, [])

  const playSound = useCallback((sound: SoundType) => {
    if (!sfxOn) return
    const audio = sfxAudioRef.current[sound]
    if (audio) {
      audio.currentTime = 0
      audio.volume = volume / 100
      audio.play().catch(() => {})
    }
  }, [sfxOn, volume])

  return (
    <AudioContext.Provider value={{ musicOn, sfxOn, volume, toggleMusic, toggleSfx, setVolume, playSound }}>
      {children}
    </AudioContext.Provider>
  )
}

export function useAudio() {
  const context = useContext(AudioContext)
  if (!context) {
    throw new Error('useAudio must be used within AudioProvider')
  }
  return context
}
```

- [ ] **Step 2: 验证构建**

```bash
npm run build
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add contexts/AudioContext.tsx
git commit -m "feat(audio): add AudioContext for global audio state"
```

---

## Task 2: 创建 MusicToggle 组件

**Files:**
- Create: `components/MusicToggle.tsx`

- [ ] **Step 1: 创建 MusicToggle 组件**

```typescript
'use client'

import { useState } from 'react'
import { useAudio } from '@/contexts/AudioContext'

export default function MusicToggle() {
  const { musicOn, volume, toggleMusic, setVolume } = useAudio()
  const [showVolume, setShowVolume] = useState(false)

  return (
    <div className="fixed bottom-20 left-4 z-40 group">
      <button
        onClick={toggleMusic}
        onMouseEnter={() => setShowVolume(true)}
        onMouseLeave={() => setShowVolume(false)}
        className="w-10 h-10 rounded-full flex items-center justify-center backdrop-blur-sm border transition-all duration-200 hover:scale-110"
        style={{
          backgroundColor: 'rgba(var(--bg-card-rgb), 0.8)',
          borderColor: 'var(--border-subtle)',
        }}
        aria-label={musicOn ? '关闭背景音乐' : '开启背景音乐'}
      >
        {musicOn ? (
          <span className="text-lg">🎵</span>
        ) : (
          <span className="text-lg">🔇</span>
        )}
      </button>

      {/* Volume slider on hover */}
      <div
        className={`absolute bottom-full left-0 mb-2 p-2 rounded-lg backdrop-blur-sm border border-[var(--border-subtle)] transition-all duration-200 ${
          showVolume ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-2 pointer-events-none'
        }`}
        style={{ backgroundColor: 'rgba(var(--bg-card-rgb), 0.9)' }}
      >
        <div className="flex items-center gap-2">
          <span className="text-xs text-[var(--text-muted)]">🔈</span>
          <input
            type="range"
            min="0"
            max="100"
            value={volume}
            onChange={(e) => setVolume(Number(e.target.value))}
            className="w-20 h-1 accent-[var(--accent-copper)]"
            aria-label="音量"
          />
          <span className="text-xs text-[var(--text-muted)]">🔊</span>
        </div>
        <p className="text-[10px] text-center text-[var(--text-muted)] mt-1">
          {musicOn ? '音乐已开启' : '音乐已关闭'}
        </p>
      </div>
    </div>
  )
}
```

Note: We use CSS variable `var(--bg-card-rgb)` for transparency. If this doesn't exist, use `24, 24, 27` (approximate value of #18181b).

- [ ] **Step 2: 验证构建**

```bash
npm run build
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add components/MusicToggle.tsx
git commit -m "feat(audio): add MusicToggle floating button"
```

---

## Task 3: 集成 AudioProvider 到 Layout

**Files:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: 读取 layout.tsx**

```bash
cat app/layout.tsx
```

- [ ] **Step 2: 添加 AudioProvider 导入**

在文件顶部 import 区域添加：
```typescript
import { AudioProvider } from '@/contexts/AudioContext'
```

- [ ] **Step 3: 用 AudioProvider 包裹 children**

找到 `<body>` 标签内的 children 包裹：
```typescript
<body>
  <AudioProvider>
    {children}
  </AudioProvider>
</body>
```

- [ ] **Step 4: 添加 MusicToggle 到 body 末尾**

在 AudioProvider 闭合标签之后，</body> 之前添加：
```typescript
<MusicToggle />
```

同时确保 MusicToggle 也被导入：
```typescript
import MusicToggle from '@/components/MusicToggle'
```

- [ ] **Step 5: 验证构建**

```bash
npm run build
```
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add app/layout.tsx
git commit -m "feat(audio): integrate AudioProvider and MusicToggle"
```

---

## Task 4: 创建 GameAudioBridge 组件

**Files:**
- Create: `components/GameAudioBridge.tsx`

- [ ] **Step 1: 创建 GameAudioBridge 组件**

```typescript
'use client'

import { useEffect } from 'react'
import { useAudio } from '@/contexts/AudioContext'

const ALLOWED_ORIGINS = [
  'http://localhost:3002',
  'http://localhost:3003',
  'http://localhost:3004',
  'https://idol.yourdomain.com',
  'https://quiz.yourdomain.com',
  'https://fate.yourdomain.com',
]

type SoundType = 'click' | 'success' | 'error' | 'achievement'

interface GameMessage {
  type: 'playSound' | 'setMusic' | 'getMusicState'
  sound?: SoundType
  value?: boolean
}

export default function GameAudioBridge() {
  const { playSound, musicOn, toggleMusic } = useAudio()

  useEffect(() => {
    const handleMessage = (event: MessageEvent) => {
      // Verify origin
      if (!ALLOWED_ORIGINS.includes(event.origin)) {
        return
      }

      const data = event.data as GameMessage

      switch (data.type) {
        case 'playSound':
          if (data.sound) {
            playSound(data.sound)
          }
          break
        case 'setMusic':
          if (typeof data.value === 'boolean') {
            if (data.value !== musicOn) {
              toggleMusic()
            }
          }
          break
        case 'getMusicState':
          // Could send state back via postMessage if needed
          break
      }
    }

    window.addEventListener('message', handleMessage)
    return () => window.removeEventListener('message', handleMessage)
  }, [playSound, musicOn, toggleMusic])

  // This component doesn't render anything
  return null
}
```

- [ ] **Step 2: 添加到 GamePage**

Modify `app/games/[slug]/page.tsx`:

在文件顶部 import 区域添加：
```typescript
import GameAudioBridge from '@/components/GameAudioBridge'
```

在 GamePage 组件的 JSX 返回中，在 `<div className="min-h-screen...">` 之后立即添加：
```typescript
<GameAudioBridge />
```

注意：这个组件应该放在 GamePage 的最前面（在导航栏之前），确保它能接收到所有游戏消息。

- [ ] **Step 3: 验证构建**

```bash
npm run build
```
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add components/GameAudioBridge.tsx app/games/[slug]/page.tsx
git commit -m "feat(audio): add GameAudioBridge for game iframe communication"
```

---

## Task 5: 创建音频文件

**Files:**
- Create: `public/audio/background.mp3`
- Create: `public/audio/sfx/click.mp3`
- Create: `public/audio/sfx/success.mp3`
- Create: `public/audio/sfx/error.mp3`
- Create: `public/audio/sfx/achievement.mp3`

- [ ] **Step 1: 创建音频目录**

```bash
mkdir -p public/audio/sfx
```

- [ ] **Step 2: 下载背景音乐**

下载一个适合的 CC0 ambient 音乐文件到 `public/audio/background.mp3`。

推荐来源（CC0 许可）：
- https://opengameart.org （搜索 "ambient" 或 "game music"）
- https://freemusicarchive.org
- https://incompetech.com

如果暂时没有合适的音频，可以使用空白文件或简单音频占位：
```bash
# macOS/Linux 生成 1kHz 音调作为占位
# Windows 用户需要用其他方式生成或跳过这步
# dd if=/dev/zero bs=1 count=1 | cat > public/audio/background.mp3
```

- [ ] **Step 3: 创建音效文件**

使用 Web Audio API 生成简单的短音效作为占位符，或者从以下来源下载：
- https://opengameart.org （搜索 "UI sounds" 或 "click"）
- https://freesound.org

临时方案：创建空的或极短的占位文件（游戏开发者稍后会替换）。

- [ ] **Step 4: 验证文件存在**

```bash
ls -la public/audio/
ls -la public/audio/sfx/
```

- [ ] **Step 5: 提交**

```bash
git add public/audio/
git commit -m "feat(audio): add audio files placeholder"
```

---

## 验证

完成后验证：

1. 启动开发服务器：`npm run dev`
2. 打开首页，左下角应显示音乐按钮 🎵
3. 点击按钮，应能听到背景音乐（或看到播放提示）
4. 刷新页面，音乐状态应保持（localStorage）
5. 切换 Tab，音乐应暂停；回来后恢复
6. Hover 音乐按钮，应显示音量滑块
7. 拖动音量滑块，音量应变化
8. 在游戏页面（/games/idol 等），打开浏览器控制台
9. 在控制台执行：`window.postMessage({ type: 'playSound', sound: 'click' }, '*')`
10. 如果有音效文件，应能听到点击音效
