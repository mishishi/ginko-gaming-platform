# UX Polish Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Polish all P0/P1/P2 UX issues identified in the UX review

**Architecture:** 8 targeted fixes across 5 files. Each task is independent and can be committed separately.

---

## Task 1: Add Real Game Screenshots to GameCard

**Files:**
- Modify: `lib/games.ts:30,44,58`
- Modify: `components/GameCard.tsx:45-51`

Currently `screenshot` is a gradient placeholder string. Replace with picsum.photos URLs seeded per game so screenshots are consistent.

- [ ] **Step 1: Replace gradient placeholders with picsum URLs in games.ts**

In `lib/games.ts`, replace the three `screenshot` fields:

```typescript
screenshot: 'https://picsum.photos/seed/idol-game/600/400',
```

Do the same for `quiz` (seed: `quiz-game`) and `fate` (seed: `fate-game`).

- [ ] **Step 2: Update GameCard screenshot rendering to use img tag instead of background**

In `components/GameCard.tsx`, replace the screenshot div:

```tsx
<div className="h-36 w-full relative overflow-hidden bg-[#12161a]">
  <img
    src={game.screenshot}
    alt={`${game.title} 截图`}
    className="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-500"
    loading="lazy"
  />
  <div className="absolute inset-0 bg-gradient-to-t from-[#1a1f24] via-transparent to-transparent opacity-70" />
</div>
```

- [ ] **Step 3: Commit**

```bash
git add lib/games.ts components/GameCard.tsx
git commit -m "feat(card): use real screenshot images instead of gradient placeholders"
```

---

## Task 2: Add Game Metadata to Game Page

**Files:**
- Modify: `app/games/[slug]/page.tsx:36-60`

Add a metadata bar showing difficulty, player count, and status below the mini nav.

- [ ] **Step 1: Add metadata bar below mini nav**

```tsx
{/* Game metadata bar */}
<div className="flex items-center justify-center gap-6 py-3 border-b border-[#1a1f24] bg-[#0a0d0f]/50">
  <span className="flex items-center gap-1.5 text-xs text-[#8a8680]">
    <span>难度</span>
    <span style={{ color: game.color }}>{'★'.repeat(game.difficulty)}{'☆'.repeat(5 - game.difficulty)}</span>
  </span>
  <span className="w-px h-3 bg-[#2a2d2f]" />
  <span className="text-xs text-[#8a8680]">
    <span className="text-[#6b6560]">玩家</span> {game.playerCount}
  </span>
  <span className="w-px h-3 bg-[#2a2d2f]" />
  <span
    className={`text-xs px-2 py-0.5 rounded ${
      game.playable
        ? 'bg-emerald-900/40 text-emerald-400'
        : 'bg-amber-900/40 text-amber-400'
    }`}
  >
    {game.playable ? '可玩' : '维护中'}
  </span>
</div>
```

- [ ] **Step 2: Commit**

```bash
git add app/games/[slug]/page.tsx
git commit -m "feat(game-page): add metadata bar with difficulty, player count, and status"
```

---

## Task 3: Navigation Active Indicator

**Files:**
- Modify: `components/NavBar.tsx:163-171`

Desktop nav links currently only change text color when active. Add a visible bottom indicator bar that is always present for the active link.

- [ ] **Step 1: Update active link style to always show indicator bar**

Change `opacity-0 group-hover:opacity-100` to:

```tsx
className={`absolute -bottom-1 left-0 right-0 h-px bg-gradient-to-r from-transparent via-current to-transparent transition-opacity duration-300 ${
  isActive ? 'opacity-100' : 'opacity-0 group-hover:opacity-100'
}`}
```

- [ ] **Step 2: Commit**

```bash
git add components/NavBar.tsx
git commit -m "feat(nav): show active indicator bar always on current page link"
```

---

## Task 4: Fullscreen Button More Visible + ESC Hint

**Files:**
- Modify: `components/GameFrame.tsx:146-161`

Fullscreen button is too hidden. Make it always visible and add ESC hint tooltip.

- [ ] **Step 1: Update fullscreen button to always be visible with tooltip**

Remove `opacity-0 hover:opacity-100 focus:opacity-100` from button className. Add a tooltip span that shows "ESC 退出" or "全屏" on hover:

```tsx
<button
  onClick={toggleFullscreen}
  className="absolute top-4 right-4 z-30 p-2.5 rounded-lg bg-[#0a0d0f]/80 backdrop-blur-sm border border-[#2a2d2f] transition-all duration-200 group"
  aria-label={isFullscreen ? '退出全屏' : '进入全屏'}
>
  {isFullscreen ? (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#e5e5e5" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M8 3v3a2 2 0 0 1-2 2H3m18 0h-3a2 2 0 0 1-2-2V3m0 18v-3a2 2 0 0 1 2-2h3M3 16h3a2 2 0 0 1 2 2v3" />
    </svg>
  ) : (
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#e5e5e5" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3" />
    </svg>
  )}
  <span className="absolute -bottom-8 left-1/2 -translate-x-1/2 px-2 py-1 bg-[#0a0d0f]/90 border border-[#2a2d2f] rounded text-[10px] text-[#8a8680] whitespace-nowrap opacity-0 group-hover:opacity-100 transition-opacity pointer-events-none">
    {isFullscreen ? 'ESC 退出' : '全屏'}
  </span>
</button>
```

- [ ] **Step 2: Commit**

```bash
git add components/GameFrame.tsx
git commit -m "feat(gameframe): make fullscreen button always visible with ESC hint tooltip"
```

---

## Task 5: 404 Page Responsive Fix

**Files:**
- Modify: `app/not-found.tsx:16`

Reduce massive "404" text on mobile.

- [ ] **Step 1: Fix 404 text size for mobile**

Change:
```tsx
className="text-[12rem] md:text-[16rem] leading-none font-serif font-bold text-transparent bg-clip-text bg-gradient-to-b from-[#d4a574] to-[#8a6d4f] opacity-30 select-none"
```
To:
```tsx
className="text-[6rem] sm:text-[10rem] md:text-[14rem] leading-none font-serif font-bold text-transparent bg-clip-text bg-gradient-to-b from-[#d4a574] to-[#8a6d4f] opacity-30 select-none"
```

- [ ] **Step 2: Commit**

```bash
git add app/not-found.tsx
git commit -m "fix(404): reduce oversized text on mobile"
```

---

## Task 6: GameCard Hover — Show Enter Button

**Files:**
- Modify: `components/GameCard.tsx:94-107`

Replace bottom accent line with an animated button that slides up on hover.

- [ ] **Step 1: Replace bottom CTA section**

Replace:
```tsx
{/* Bottom accent line */}
<div className="flex items-center gap-3">
  <div
    className="h-px flex-1"
    style={{
      background: `linear-gradient(90deg, ${game.color}60, transparent)`,
    }}
  />
  <span
    className="text-xs opacity-60 group-hover:opacity-100 transition-opacity duration-300"
    style={{ color: game.color }}
  >
    进入游戏
  </span>
</div>
```
With:
```tsx
<div className="flex items-center justify-center opacity-0 group-hover:opacity-100 translate-y-2 group-hover:translate-y-0 transition-all duration-300">
  <span
    className="px-4 py-1.5 rounded text-xs font-medium border"
    style={{
      color: game.color,
      borderColor: `${game.color}40`,
      backgroundColor: `${game.color}10`,
    }}
  >
    进入游戏
  </span>
</div>
```

- [ ] **Step 2: Commit**

```bash
git add components/GameCard.tsx
git commit -m "feat(card): show enter-game button with slide-up animation on hover"
```

---

## Task 7: Footer Enhancement

**Files:**
- Modify: `app/page.tsx:72-131`

Add brand story, quick links column, and contact info to footer.

- [ ] **Step 1: Replace footer with 3-column layout**

Replace the entire `<footer>` block with a 3-column grid footer (brand, quick links, contact).

- [ ] **Step 2: Commit**

```bash
git add app/page.tsx
git commit -m "feat(footer): enhance with brand info, quick links, and contact section"
```

---

## Task 8: Staggered Entrance Animations

**Files:**
- Modify: `app/globals.css`
- Modify: `app/page.tsx`

Add more stagger classes and apply to remaining elements.

- [ ] **Step 1: Add stagger-5 through stagger-7 to globals.css**

```css
.stagger-5 { animation-delay: 400ms; }
.stagger-6 { animation-delay: 500ms; }
.stagger-7 { animation-delay: 600ms; }
```

- [ ] **Step 2: Apply to page elements**

Add stagger-5 to "珍藏展品" heading, stagger classes to footer sections.

- [ ] **Step 3: Commit**

```bash
git add app/globals.css app/page.tsx app/games/[slug]/page.tsx
git commit -m "feat(animations): add staggered entrance animations to footer and game page metadata"
```

---

## Verification

After all tasks, run:

```bash
npm run build
```

Expected: all 7 static pages compile successfully.
